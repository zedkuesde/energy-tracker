# Example only: never put real secrets in this file.
# Vite (dev): http://localhost:5173 — proxies /api and /health to Fastify.
# Fastify: APP_PORT (default 3000).
DATABASE_PATH=./data/energy-tracker.sqlite
APP_PORT=3000
AUTH_PASSWORD_HASH=
AUTH_SESSION_SECRET=
